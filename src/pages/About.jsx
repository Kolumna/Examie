import logo from "../assets/svgs/logo.svg";

export default function About(props) {
  return (
    <div className="h-screen flex justify-center items-center">
      <img width={360} src={logo} />
    </div>
  );
}
